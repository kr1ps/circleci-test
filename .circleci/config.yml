version: '2.1'
orbs:
  grype: 'anchore/grype@0.1.0'
jobs:
  list-container-vulnerabilities:
    executor: grype/default
    steps:
      - grype/list-image-vulns:
          image-name: 'kr1ps/mairejava:latest'
workflows:
  scan-my-image:
    jobs:
      - list-container-vulnerabilities
